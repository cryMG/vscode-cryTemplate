{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Extension (cryTemplate)",
      "type": "extensionHost",
      "request": "launch",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}",
        "${workspaceFolder}/examples"
      ]
    },
    {
      "name": "Run Extension (cryTemplate) – Open Workspace Root",
      "type": "extensionHost",
      "request": "launch",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}",
        "${workspaceFolder}/.."
      ]
    }
  ]
}
